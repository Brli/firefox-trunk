Description: Allow use of lockPref in all preference files
Author: Alexander Sack <asac@ubuntu.com>
Bug: https://bugzilla.mozilla.org/show_bug.cgi?id=467738
Forwarded: https://bugzilla.mozilla.org/attachment.cgi?id=351145

Index: firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefapi.cpp
===================================================================
--- firefox-trunk-58.0~a1~hg20171010r385288.orig/modules/libpref/prefapi.cpp
+++ firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefapi.cpp
@@ -1086,7 +1086,8 @@
                     PrefValue aValue,
                     PrefType aType,
                     bool aIsDefault,
-                    bool aIsStickyDefault)
+                    bool aIsStickyDefault,
+                    bool aIsLocked)
 {
   uint32_t flags = 0;
   if (aIsDefault) {
@@ -1098,4 +1099,6 @@
     flags |= kPrefForceSet;
   }
   pref_HashPref(aPref, aValue, aType, flags);
+  if (aIsLocked)
+    PREF_LockPref(aPref, true);
 }
Index: firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefapi.h
===================================================================
--- firefox-trunk-58.0~a1~hg20171010r385288.orig/modules/libpref/prefapi.h
+++ firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefapi.h
@@ -251,7 +251,8 @@
                     PrefValue aValue,
                     PrefType aType,
                     bool aIsDefault,
-                    bool aIsStickyDefault);
+                    bool aIsStickyDefault,
+                    bool aIsLocked);
 
 // Callback for whenever we change a preference.
 typedef void (*PrefsDirtyFunc)();
Index: firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefread.cpp
===================================================================
--- firefox-trunk-58.0~a1~hg20171010r385288.orig/modules/libpref/prefread.cpp
+++ firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefread.cpp
@@ -47,6 +47,7 @@
 #define BITS_PER_HEX_DIGIT 4
 
 static const char kUserPref[] = "user_pref";
+static const char kLockPref[] = "lockPref";
 static const char kPref[] = "pref";
 static const char kPrefSticky[] = "sticky_pref";
 static const char kTrue[] = "true";
@@ -154,7 +155,8 @@
                   value,
                   aPS->mVtype,
                   aPS->mIsDefault,
-                  aPS->mIsStickyDefault);
+                  aPS->mIsStickyDefault,
+                  aPS->mIsLock);
   return true;
 }
 
@@ -225,6 +227,7 @@
           aPS->mVtype = PrefType::Invalid;
           aPS->mIsDefault = false;
           aPS->mIsStickyDefault = false;
+          aPS->mIsLock = false;
         }
         switch (c) {
           case '/': // begin comment block or line?
@@ -235,11 +238,14 @@
             break;
           case 'u': // indicating user_pref
           case 's': // indicating sticky_pref
+          case 'l': // indicating lockPref
           case 'p': // indicating pref
             if (c == 'u') {
               aPS->mStrMatch = kUserPref;
             } else if (c == 's') {
               aPS->mStrMatch = kPrefSticky;
+            } else if (c == 'l') {
+              aPS->mStrMatch = kLockPref;
             } else {
               aPS->mStrMatch = kPref;
             }
@@ -288,8 +294,9 @@
       case PREF_PARSE_UNTIL_NAME:
         if (c == '\"' || c == '\'') {
           aPS->mIsDefault =
-            (aPS->mStrMatch == kPref || aPS->mStrMatch == kPrefSticky);
+            (aPS->mStrMatch == kPref || aPS->mStrMatch == kPrefSticky || aPS->mStrMatch == kLockPref);
           aPS->mIsStickyDefault = (aPS->mStrMatch == kPrefSticky);
+          aPS->mIsLock = (aPS->mStrMatch == kLockPref);
           aPS->mQuoteChar = c;
           aPS->mNextState = PREF_PARSE_UNTIL_COMMA; // return here when done
           state = PREF_PARSE_QUOTED_STRING;
Index: firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefread.h
===================================================================
--- firefox-trunk-58.0~a1~hg20171010r385288.orig/modules/libpref/prefread.h
+++ firefox-trunk-58.0~a1~hg20171010r385288/modules/libpref/prefread.h
@@ -28,7 +28,8 @@
                            PrefValue aValue,
                            PrefType aType,
                            bool aIsDefault,
-                           bool aIsStickyDefault);
+                           bool aIsStickyDefault,
+                           bool aIsLocked);
 
 // Report any errors or warnings we encounter during parsing.
 typedef void (*PrefParseErrorReporter)(const char* aMessage,
@@ -56,6 +57,7 @@
   PrefType mVtype;       // PREF_{STRING,INT,BOOL}
   bool mIsDefault;       // true if (default) pref
   bool mIsStickyDefault; // true if (sticky) pref
+  bool mIsLock;          // true if (locked) pref
 } PrefParseState;
 
 // Initialize a PrefParseState instance.

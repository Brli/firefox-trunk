Description: Add "Ubuntu" to the platform part of the UA string
Author: Chris Coulson <chris.coulson@canonical.com>
Forwarded: not-needed

Index: firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/nsHttpHandler.cpp
===================================================================
--- firefox-trunk-25.0~a1~hg20130724r139815.orig/netwerk/protocol/http/nsHttpHandler.cpp	2013-07-25 11:45:24.233183758 +0100
+++ firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/nsHttpHandler.cpp	2013-07-25 11:45:24.229183758 +0100
@@ -176,6 +176,9 @@
     , mLegacyAppName("Mozilla")
     , mLegacyAppVersion("5.0")
     , mProduct("Gecko")
+#ifdef MOZ_UA_VENDOR
+    , mVendor(MOZ_UA_VENDOR)
+#endif
     , mUserAgentIsDirty(true)
     , mUseCache(true)
     , mPromptTempRedirect(true)
@@ -325,6 +328,9 @@
     LOG(("> misc = %s\n", mMisc.get()));
     LOG(("> product = %s\n", mProduct.get()));
     LOG(("> product-sub = %s\n", mProductSub.get()));
+#ifdef MOZ_UA_VENDOR
+    LOG(("> vendor = %s\n", mVendor.get()));
+#endif
     LOG(("> app-name = %s\n", mAppName.get()));
     LOG(("> app-version = %s\n", mAppVersion.get()));
     LOG(("> compat-firefox = %s\n", mCompatFirefox.get()));
@@ -594,7 +600,10 @@
                            mAppVersion.Length() +
                            mCompatFirefox.Length() +
                            mCompatDevice.Length() +
-                           13);
+#ifdef MOZ_UA_VENDOR
+                           mVendor.Length() + 
+#endif
+                           15);
 
     // Application portion
     mUserAgent.Assign(mLegacyAppName);
@@ -610,6 +619,10 @@
       mUserAgent.AppendLiteral("; ");
     }
 #endif
+#ifdef MOZ_UA_VENDOR
+    mUserAgent += mVendor;
+    mUserAgent.AppendLiteral("; ");
+#endif
     if (!mCompatDevice.IsEmpty()) {
         mUserAgent += mCompatDevice;
         mUserAgent.AppendLiteral("; ");
Index: firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/nsHttpHandler.h
===================================================================
--- firefox-trunk-25.0~a1~hg20130724r139815.orig/netwerk/protocol/http/nsHttpHandler.h	2013-07-25 11:45:24.233183758 +0100
+++ firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/nsHttpHandler.h	2013-07-25 11:45:24.229183758 +0100
@@ -374,6 +374,7 @@
     // useragent components
     nsCString      mLegacyAppName;
     nsCString      mLegacyAppVersion;
+    nsCString      mVendor;
     nsCString      mPlatform;
     nsCString      mOscpu;
     nsCString      mMisc;
Index: firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/Makefile.in
===================================================================
--- firefox-trunk-25.0~a1~hg20130724r139815.orig/netwerk/protocol/http/Makefile.in	2013-07-25 11:44:01.000000000 +0100
+++ firefox-trunk-25.0~a1~hg20130724r139815/netwerk/protocol/http/Makefile.in	2013-07-25 11:49:42.441185100 +0100
@@ -26,3 +26,6 @@
 include $(topsrcdir)/config/rules.mk
 include $(topsrcdir)/ipc/chromium/chromium-config.mk
 
+ifdef MOZ_UA_VENDOR
+DEFINES += -DMOZ_UA_VENDOR=\""$(MOZ_UA_VENDOR)"\"
+endif

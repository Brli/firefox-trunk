Description: Add "Ubuntu" to the platform part of the UA string
Author: Chris Coulson <chris.coulson@canonical.com>
Forwarded: not-needed

Index: firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/nsHttpHandler.cpp
===================================================================
--- firefox-trunk-26.0~a1~hg20130907r146034.orig/netwerk/protocol/http/nsHttpHandler.cpp	2013-09-07 19:43:20.579041886 +0100
+++ firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/nsHttpHandler.cpp	2013-09-07 19:43:20.571041886 +0100
@@ -177,6 +177,9 @@
     , mLegacyAppName("Mozilla")
     , mLegacyAppVersion("5.0")
     , mProduct("Gecko")
+#ifdef MOZ_UA_VENDOR
+    , mVendor(MOZ_UA_VENDOR)
+#endif
     , mUserAgentIsDirty(true)
     , mUseCache(true)
     , mPromptTempRedirect(true)
@@ -330,6 +333,9 @@
     LOG(("> misc = %s\n", mMisc.get()));
     LOG(("> product = %s\n", mProduct.get()));
     LOG(("> product-sub = %s\n", mProductSub.get()));
+#ifdef MOZ_UA_VENDOR
+    LOG(("> vendor = %s\n", mVendor.get()));
+#endif
     LOG(("> app-name = %s\n", mAppName.get()));
     LOG(("> app-version = %s\n", mAppVersion.get()));
     LOG(("> compat-firefox = %s\n", mCompatFirefox.get()));
@@ -607,7 +613,10 @@
                            mAppVersion.Length() +
                            mCompatFirefox.Length() +
                            mCompatDevice.Length() +
-                           13);
+#ifdef MOZ_UA_VENDOR
+                           mVendor.Length() + 
+#endif
+                           15);
 
     // Application portion
     mUserAgent.Assign(mLegacyAppName);
@@ -623,6 +632,10 @@
       mUserAgent.AppendLiteral("; ");
     }
 #endif
+#ifdef MOZ_UA_VENDOR
+    mUserAgent += mVendor;
+    mUserAgent.AppendLiteral("; ");
+#endif
     if (!mCompatDevice.IsEmpty()) {
         mUserAgent += mCompatDevice;
         mUserAgent.AppendLiteral("; ");
Index: firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/nsHttpHandler.h
===================================================================
--- firefox-trunk-26.0~a1~hg20130907r146034.orig/netwerk/protocol/http/nsHttpHandler.h	2013-09-07 19:43:20.579041886 +0100
+++ firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/nsHttpHandler.h	2013-09-07 19:43:20.571041886 +0100
@@ -374,6 +374,7 @@
     // useragent components
     nsCString      mLegacyAppName;
     nsCString      mLegacyAppVersion;
+    nsCString      mVendor;
     nsCString      mPlatform;
     nsCString      mOscpu;
     nsCString      mMisc;
Index: firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/Makefile.in
===================================================================
--- firefox-trunk-26.0~a1~hg20130907r146034.orig/netwerk/protocol/http/Makefile.in	2013-09-07 19:42:36.063041412 +0100
+++ firefox-trunk-26.0~a1~hg20130907r146034/netwerk/protocol/http/Makefile.in	2013-09-07 19:45:16.687043121 +0100
@@ -12,3 +12,7 @@
 
 include $(topsrcdir)/config/rules.mk
 include $(topsrcdir)/ipc/chromium/chromium-config.mk
+
+ifdef MOZ_UA_VENDOR
+DEFINES += -DMOZ_UA_VENDOR=\""$(MOZ_UA_VENDOR)"\"
+endif

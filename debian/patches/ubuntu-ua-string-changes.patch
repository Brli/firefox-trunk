Description: Add "Ubuntu" to the platform part of the UA string
Author: Chris Coulson <chris.coulson@canonical.com>
Forwarded: not-needed

Index: firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/Makefile.in
===================================================================
--- firefox-trunk-16.0~a1~hg20120611r96334.orig/netwerk/protocol/http/Makefile.in	2012-06-11 03:10:30.000000000 +0100
+++ firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/Makefile.in	2012-06-11 10:34:03.551217851 +0100
@@ -97,3 +97,7 @@
 include $(topsrcdir)/config/rules.mk
 
 DEFINES += -DIMPL_NS_NET
+
+ifdef MOZ_UA_VENDOR
+DEFINES += -DMOZ_UA_VENDOR=\""$(MOZ_UA_VENDOR)"\"
+endif
Index: firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/nsHttpHandler.cpp
===================================================================
--- firefox-trunk-16.0~a1~hg20120611r96334.orig/netwerk/protocol/http/nsHttpHandler.cpp	2012-06-11 03:10:31.000000000 +0100
+++ firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/nsHttpHandler.cpp	2012-06-11 10:36:29.655221222 +0100
@@ -160,6 +160,9 @@
     , mLegacyAppName("Mozilla")
     , mLegacyAppVersion("5.0")
     , mProduct("Gecko")
+#ifdef MOZ_UA_VENDOR
+    , mVendor(MOZ_UA_VENDOR)
+#endif
     , mUserAgentIsDirty(true)
     , mUseCache(true)
     , mPromptTempRedirect(true)
@@ -270,6 +273,9 @@
     LOG(("> misc = %s\n", mMisc.get()));
     LOG(("> product = %s\n", mProduct.get()));
     LOG(("> product-sub = %s\n", mProductSub.get()));
+#ifdef MOZ_UA_VENDOR
+    LOG(("> vendor = %s\n", mVendor.get()));
+#endif
     LOG(("> app-name = %s\n", mAppName.get()));
     LOG(("> app-version = %s\n", mAppVersion.get()));
     LOG(("> compat-firefox = %s\n", mCompatFirefox.get()));
@@ -525,6 +531,9 @@
     // than if we didn't preallocate at all.
     mUserAgent.SetCapacity(mLegacyAppName.Length() +
                            mLegacyAppVersion.Length() +
+#ifdef MOZ_UA_VENDOR
+                           mVendor.Length() + 
+#endif
 #ifndef UA_SPARE_PLATFORM
                            mPlatform.Length() +
 #endif
@@ -536,7 +545,7 @@
                            mAppVersion.Length() +
                            mCompatFirefox.Length() +
                            mCompatDevice.Length() +
-                           13);
+                           15);
 
     // Application portion
     mUserAgent.Assign(mLegacyAppName);
@@ -556,6 +565,10 @@
         mUserAgent.AppendLiteral("; ");
     }
 #else
+#ifdef MOZ_UA_VENDOR
+    mUserAgent += mVendor;
+    mUserAgent.AppendLiteral("; ");
+#endif
     mUserAgent += mOscpu;
     mUserAgent.AppendLiteral("; ");
 #endif
Index: firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/nsHttpHandler.h
===================================================================
--- firefox-trunk-16.0~a1~hg20120611r96334.orig/netwerk/protocol/http/nsHttpHandler.h	2012-06-11 03:10:31.000000000 +0100
+++ firefox-trunk-16.0~a1~hg20120611r96334/netwerk/protocol/http/nsHttpHandler.h	2012-06-11 10:34:03.551217851 +0100
@@ -323,6 +323,7 @@
     // useragent components
     nsCString      mLegacyAppName;
     nsCString      mLegacyAppVersion;
+    nsCString      mVendor;
     nsCString      mPlatform;
     nsCString      mOscpu;
     nsCString      mMisc;

Description: Add "Ubuntu" to the platform part of the UA string
Author: Chris Coulson <chris.coulson@canonical.com>
Forwarded: not-needed

Index: firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/nsHttpHandler.cpp
===================================================================
--- firefox-trunk-28.0~a1~hg20131028r152546.orig/netwerk/protocol/http/nsHttpHandler.cpp	2013-10-29 16:52:28.346672174 +0000
+++ firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/nsHttpHandler.cpp	2013-10-29 16:52:28.342672174 +0000
@@ -173,6 +173,9 @@
     , mLegacyAppName("Mozilla")
     , mLegacyAppVersion("5.0")
     , mProduct("Gecko")
+#ifdef MOZ_UA_VENDOR
+    , mVendor(MOZ_UA_VENDOR)
+#endif
     , mUserAgentIsDirty(true)
     , mUseCache(true)
     , mPromptTempRedirect(true)
@@ -332,6 +335,9 @@
     LOG(("> misc = %s\n", mMisc.get()));
     LOG(("> product = %s\n", mProduct.get()));
     LOG(("> product-sub = %s\n", mProductSub.get()));
+#ifdef MOZ_UA_VENDOR
+    LOG(("> vendor = %s\n", mVendor.get()));
+#endif
     LOG(("> app-name = %s\n", mAppName.get()));
     LOG(("> app-version = %s\n", mAppVersion.get()));
     LOG(("> compat-firefox = %s\n", mCompatFirefox.get()));
@@ -609,7 +615,10 @@
                            mAppVersion.Length() +
                            mCompatFirefox.Length() +
                            mCompatDevice.Length() +
-                           13);
+#ifdef MOZ_UA_VENDOR
+                           mVendor.Length() + 
+#endif
+                           15);
 
     // Application portion
     mUserAgent.Assign(mLegacyAppName);
@@ -625,6 +634,10 @@
       mUserAgent.AppendLiteral("; ");
     }
 #endif
+#ifdef MOZ_UA_VENDOR
+    mUserAgent += mVendor;
+    mUserAgent.AppendLiteral("; ");
+#endif
     if (!mCompatDevice.IsEmpty()) {
         mUserAgent += mCompatDevice;
         mUserAgent.AppendLiteral("; ");
Index: firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/nsHttpHandler.h
===================================================================
--- firefox-trunk-28.0~a1~hg20131028r152546.orig/netwerk/protocol/http/nsHttpHandler.h	2013-10-29 16:52:28.346672174 +0000
+++ firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/nsHttpHandler.h	2013-10-29 16:52:28.342672174 +0000
@@ -374,6 +374,7 @@
     // useragent components
     nsCString      mLegacyAppName;
     nsCString      mLegacyAppVersion;
+    nsCString      mVendor;
     nsCString      mPlatform;
     nsCString      mOscpu;
     nsCString      mMisc;
Index: firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/Makefile.in
===================================================================
--- firefox-trunk-28.0~a1~hg20131028r152546.orig/netwerk/protocol/http/Makefile.in	2013-10-29 16:48:30.334670290 +0000
+++ firefox-trunk-28.0~a1~hg20131028r152546/netwerk/protocol/http/Makefile.in	2013-10-29 16:52:57.294672403 +0000
@@ -11,3 +11,7 @@
   $(NULL)
 
 include $(topsrcdir)/config/rules.mk
+
+ifdef MOZ_UA_VENDOR
+DEFINES += -DMOZ_UA_VENDOR=\""$(MOZ_UA_VENDOR)"\"
+endif
